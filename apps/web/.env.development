# Desarrollo por defecto: adapter fake en el navegador (no requiere FastAPI levantado).
# Solo tiene efecto con `vite dev`; en `vite build` el mock queda fuera del bundle
# porque el switch está detrás de `import.meta.env.DEV`.
VITE_REMITIA_API=mock
